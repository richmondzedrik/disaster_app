services:
  - type: web
    name: disaster-app
    env: node
    buildCommand: frontend\build.bat
    startCommand: cd frontend && npm install -g serve && serve -s dist
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: http://localhost:3000